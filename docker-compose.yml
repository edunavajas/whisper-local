version: '3.8'

services:
  whisper-api:
    build: ./api
    ports:
      - "8000:8000"
    volumes:
      - ./api:/app
      - whisper-models:/root/.cache/whisper
    restart: unless-stopped

volumes:
  whisper-models: